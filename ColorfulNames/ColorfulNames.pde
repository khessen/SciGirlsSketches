void setup()
{
  // Set the size of your sketch:
  size( , );
  
  // Set the text size:
  textSize(  );
}

void draw()
{
  // Use 'fill([red], [green], [blue])' to set the color of your first letter:
  fill( , , );
  // Use 'text("[letter]", [xPosition], [yPosition])' to display a letter:
  text("", , );
  
  // Try running the sketch and make sure that the first letter shows up.
  // (Feel free to change the sketch size and text size!)
  
  // Once you have made sure that you can run it with one letter,
  // do the same thing below to add the rest of the letters:
  
}
